<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kano.project.provider.dao.UserDao">

    <resultMap id="BaseResultMap" type="com.kano.project.provider.entity.User">
        <!--@Table user-->
        <result property="userid" column="userId" jdbcType="INTEGER"/>
        <result property="username" column="userName" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          userId, userName
        from KanoTestBase.user
        where userId = #{userid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          userId, userName
        from KanoTestBase.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          userId, userName
        from KanoTestBase.user
        <where>
            <if test="userid != null">
                and userId = #{userid}
            </if>
            <if test="username != null and username != ''">
                and userName = #{username}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="userid" useGeneratedKeys="true">
        insert into KanoTestBase.user(userName)
        values (#{username})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update KanoTestBase.user
        <set>
            <if test="username != null and username != ''">
                userName = #{username},
            </if>
        </set>
        where userId = #{userid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from KanoTestBase.user where userId = #{userid}
    </delete>

</mapper>